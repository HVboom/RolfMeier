<!DOCTYPE html>
<html lang="de">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=Edge,chrome=1">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <title>Rolf Meier - Inneneinrichtungen<%= content_for?(:title) ? " - " + yield(:title) : "" %></title>

    <!-- Le HTML5 shim, for IE6-8 support of HTML elements -->
    <!--[if lt IE 9]>
      <script src="http://html5shim.googlecode.com/svn/trunk/html5.js" type="text/javascript"></script>
    <![endif]-->

    <%= csrf_meta_tags %>
    <%= render "layouts/meta" %>
    <%= yield(:head) %>

    <!-- Le fav and touch icons -->
    <link rel="shortcut icon" href="images/favicon.ico">
    <link rel="apple-touch-icon-precomposed" sizes="144x144" href="images/apple-touch-icon-144-precomposed.png">
    <link rel="apple-touch-icon-precomposed" sizes="114x114" href="images/apple-touch-icon-114-precomposed.png">
    <link rel="apple-touch-icon-precomposed" sizes="72x72" href="images/apple-touch-icon-72-precomposed.png">
    <link rel="apple-touch-icon-precomposed" href="images/apple-touch-icon-57-precomposed.png">

    <!-- Le fav and touch icons -->
    <%= stylesheet_link_tag "pages", :media => "all" %>
    <%= stylesheet_link_tag "rcarousel", :media => "all" %>
    <%= stylesheet_link_tag "ui-lightness/jquery-ui-1.8.16.custom", :media => "all" %>
    <%= stylesheet_link_tag "lightbox", :media => "all" %>
  </head>

  <body id="rolf_meier">
    <header class="row-fluid span12">
      <span class="logo span4">
        <%= link_to(image_tag("logo_rolf-meier.png", :size => "212x66", :alt => "Rolf Meier - Inneneinrichtungen", :boarder => 0), root_url()) %>
      </span>

      <nav class="offset2">
        <%= render :partial => 'menu', :locals => { :page => @page } %>
      </nav>
    </header>

    <div class="row-fluid span12">
      <section id="main" role="main">
        <% if(@page.slide_show? || @page.map?) %>
          <aside class="<%= @page.content_type %> span8">
            <header>
              <h1><%= yield(:title) %></h1>
            </header>

            <div class="container-fluid">
              <%= yield(:slides) %>
            <div>
          </aside>

          <article class="<%= @page.content_type %> span4">
            <%= yield %>
          </article>
        <% else %>
          <article class="<%= @page.content_type %> span8">
            <header>
              <h1><%= yield(:title) %></h1>
            </header>

            <div>
              <%= yield(:content) %>
            </div>
          </article>

          <aside class="<%= @page.content_type %> span4">
            <%= yield %>
          </aside>
        <% end %>
      </section>
    </div>

    <footer class="row-fluid span12">
      <span class="logo span4">
        <%= image_tag("logo_wohntraeume.png", :size => "212x19", :alt => "Wir kreieren Wohnträume") %>
      </span>

      <nav>
        <ul>
          <li>
            <% if Page.contact %>
              <%= link_to(page_path(Page.contact)) do %>
                <span class="nowrap">&copy; 1999 - <%= Date.today.year %></span>
                <span class="nowrap">Rolf Meier Inneneinrichtungen AG</span>
                |
                <span class="nowrap">044-724 39 19</span>
              <% end %>
            <% else %>
                <span class="nowrap">&copy; 1999 - <%= Date.today.year %></span>
                <span class="nowrap">Rolf Meier Inneneinrichtungen AG</span>
                |
                <span class="nowrap">044-724 39 19</span>
            <% end %>
          </li>
          |
          <li>
            <%= mail_to("info@rolf-meier.ch", nil, :class => "email") %>
            <%# markdown("<info@rolf-meier.ch>") %>
          </li>
          |
          <li>
            <% if Page.impressum %>
              <%= link_to('impressum', page_path(Page.impressum)) %>
            <% else %>
              impressum
            <% end %>
          </li>
        </ul>
      </nav>
    </footer>

    <!-- local srcipts -->
    <%= javascript_include_tag "pages" %>
  </body>
</html>
